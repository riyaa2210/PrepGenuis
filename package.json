{
  "name": "prepgenuis",
  "version": "1.0.0",
  "description": "AI Interview Coach & Recruiter Platform",
  "scripts": {
    "start": "cd backend && npm start",
    "install": "cd backend && npm install",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
